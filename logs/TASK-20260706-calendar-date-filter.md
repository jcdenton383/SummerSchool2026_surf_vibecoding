# TASK-20260706-calendar-date-filter — Визуальный календарь для фильтра дат расписания

## Статус

`resolved`

## Тип

`feature`

## Связь с ТЗ и требованиями

- `TZ_application/02-screen-specification.md` — SCR-003, SCR-004.
- `api/openapi.yaml` — `GET /slots?from={date}&to={date}`.
- `screen_flowchart.md` — переходы расписания и фильтра дат.

## Цель

Заменить ручной ввод дат в фильтре расписания на визуальный выбор через календарь или календарный picker.

## Не входит в задачу

- Не входит расширение фильтров beyond MVP.
- Не входит фильтрация по направлению, шефу, цене или сложности.
- Не входит изменение API: остаются параметры `from` и `to`.

## Изменённые артефакты

- `logs/TASK-20260706-calendar-date-filter.md` — создан отчёт задачи.
- `frontend/app/schedule/index.tsx` — ручные текстовые поля заменены на простой визуальный календарный выбор дат.
- `design/1.1-calendar-date-filter-feature.md` — добавлено описание фичи версии 1.1.

## Журнал работы

### 2026-07-06 03:53 — анализ

#### Промпт пользователя

> таск: фильтр дат лучше сделать выбором на выпадающем календаре, визуально, а не заставлять пользователя печатать даты вручную.

#### Краткие действия Codex

- Проверил `frontend/app/schedule/index.tsx`.
- Подтвердил, что фильтр реализован двумя `TextField` с ручным вводом `from` и `to`.
- Классифицировал как UX-задачу, потому что текущее поведение не является поломкой API, но ухудшает использование.

#### Изменённые артефакты

- `logs/TASK-20260706-calendar-date-filter.md` — создан отчёт задачи.

#### Связь с ТЗ

- `TZ_application/02-screen-specification.md` — SCR-004 фильтр дат.
- `api/openapi.yaml` — формат параметров остаётся `date`.

#### Тесты и проверки

- `Get-Content -LiteralPath 'frontend/app/schedule/index.tsx' -Encoding UTF8`
- Результат: используются поля ручного ввода дат.

#### Следующие шаги

- Выбрать библиотеку календаря, совместимую с Expo/React Native, или реализовать простой MVP date picker.
- Хранить выбранные даты в формате `YYYY-MM-DD`.
- Валидировать, что `to >= from`.
- Оставить сброс фильтра.
- Проверить Android-сценарий выбора дат.

### 2026-07-06 04:11 — реализация

#### Промпт пользователя

> баги необходимо пофиксить, а таски - выполнить.

#### Краткие действия Codex

- Заменил ручной ввод дат на простой визуальный календарный выбор ближайших 14 дней.
- Добавил проверку порядка дат.
- Добавил проектный документ `design/1.1-calendar-date-filter-feature.md`.

#### Тесты и проверки

- `npm.cmd run typecheck` — успешно.
- `npm.cmd test` — успешно, 5 тестов пройдено.

## Итоговое решение

Фича реализована без новой зависимости: в расписании добавлен визуальный выбор дат из ближайших 14 дней, значения отправляются в API в формате `YYYY-MM-DD`, сброс фильтра сохранён.

## Оставшиеся риски

- Добавление библиотеки может потребовать проверки совместимости с текущей версией Expo.
- В MVP нельзя расширять фильтр за пределы дат без отдельного решения.
- Текущая реализация календаря минимальная; если потребуется полноценный месячный календарь, это отдельная UI-задача.
